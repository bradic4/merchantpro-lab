# Server-only credentials. Use separate isolated preview and production databases.
TELEMETRY_DATABASE_URL=
TELEMETRY_READ_DATABASE_URL=
TELEMETRY_MAINTENANCE_DATABASE_URL=
# CLI/CI only; do not expose migration credentials to the frontend.
TELEMETRY_MIGRATION_DATABASE_URL=
TELEMETRY_PERSIST_ENABLED=false
# Enable only after comparing actual production logs with persisted receipts.
TELEMETRY_READ_ENABLED=false
CRON_SECRET=
